const numero = prompt("Digite algum número");
const numero2 = prompt("Digite outro número");

const conversor = Number(numero);
const conversor2 = Number(numero2)

const conta = conversor + conversor2;

alert(`A soma entre ${numero} e ${numero2} é igual a ${conta}`);